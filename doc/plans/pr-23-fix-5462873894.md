# PR 23 fix plan: review round 1 (comment 5462873894)

**Status:** planned (not yet implemented)

- PR: https://github.com/tlkahn/vaultsync/pull/23 ("Issue 17: retire s3_integ_list_paginates CI skip under I20 head concurrency (live proof + harness cleanup + deterministic overlap gauges)")
- Comment: [issuecomment-5462873894](https://github.com/tlkahn/vaultsync/pull/23#issuecomment-5462873894) (code review, verdict "approve once F1 is addressed (or explicitly deferred) and the F2 hygiene items are cleaned up").
- Branch: `fix/issue-17-paginate-concurrency` (PR still OPEN; fixes land on the same branch and update the PR in place).
- Verified baseline at the review tip `1691251`: `cargo test --offline --lib --bins` 408 passed / 0 failed / 1 ignored; the two un-ignored gauge tests 5x green; `cargo clippy --all-targets -- -D warnings` clean; `cargo fmt --check` clean (reviewer-recorded; re-confirm at implementation start).
- Implementation discipline: **strict fine-grained TDD** for every behavior change (W160/W161, and W164 if taken) - write the failing test / pin first, observe RED, implement the minimum to reach GREEN, then refactor under green. Each behavior finding lands as ONE commit containing its RED-verified pin(s) + GREEN impl (the RED state is confirmed locally before the impl is written, never pushed standalone; keeps every pushed commit bisectable and green). Docs / PR-body / decision-log changes (W162, W163) are not behavior changes and land under the all-green gate only.
- Per-cycle gate: `cargo fmt --check` + `cargo clippy --all-targets --locked -- -D warnings` + `cargo test --locked --lib --bins` (integration suite stays env-gated to CI; F5's clamp, if taken, is exercised live there but pinned offline by a pure helper test).
- Work items continue the project W-series (highest used on the issue-17 track: **W159**; this plan starts at **W160**).

---

## Findings -> verdicts -> work items

All five findings re-verified against the tree at `1691251`. None refuted. F3's branch-protection question was answered via the API (see R23-f3).

| Finding | Verdict | Work item |
| ------- | ------- | --------- |
| F1 (moderate) `OverlapRendezvous.released` latches permanently; both gauge tests route their concurrency-1 *comparison* leg through the same gauged store after the concurrency-N leg, so they only pass because N ran first. Conc-1 first (or alone) blocks each `enter()` until the 5s timeout and panics with a misleading "deadlock" message. Same trap already forced `enrich_parallel_vanished_warning_order_stable` onto bare `MemoryStore` (comment documents it); the two gauges carry the constraint silently | CONFIRMED (`src/lib.rs` `OverlapRendezvous::enter` sets `released = true` and never clears it; `src/store/mod.rs` `enrich_heads_bounded_parallel` and `src/exec.rs` `exec_parallel_downloads_overlap` both call the gauged path at concurrency 1 for equality). Reviewer reordered to conc-1-first and hit the panic | W160 (RED pin) + W161 (GREEN) |
| F2 (minor) PR body is stale vs tip: lists 7 commits / `with_store_conc(..., 32)` / "~54s"; tip has 8th commit `1691251` (P30: `seed_workers = 64`, `K = 128`, ~18-24s). Body links `doc/plans/issue-17.md`; roadmap cites `doc/plans/push-pagination-test-below-30s-total.md`; neither is committed (dead links). Repo convention tracks plan docs | CONFIRMED (PR body via `gh pr view 23`; `git status` shows both plans untracked; `git ls-files doc/plans/` has `issue-{5,6,15}.md` and the `pr-*` series tracked) | W162 (hygiene; no RED/GREEN) |
| F3 (minor) CI job rename `integration (S3, PR gate)` -> `integration (S3)` and deletion of `integration (S3, nightly full)` may break required status checks if branch protection still lists the old names | CONFIRMED rename in `.github/workflows/ci.yml`; **confirmed safe**: `GET .../branches/main/protection/required_status_checks` returns 404 "Required status checks not enabled"; rulesets empty. Current PR 23 checks already report under the new name `integration (S3)` and pass. No code change | W165 reply only (record the verification) |
| F4 (nit) `src/exec.rs` `GaugedGetStore` comment says "Measures get_to overlap (bytes streaming)"; `enter()` blocks *before* the inner call, so pre-release `max_in_flight` counts rendezvous arrivals, not concurrent streaming. Test actually proves pool fan-out | CONFIRMED (comment at `GaugedGetStore`; `enter()` is the first line of `get_to`) | W163 (comment only) |
| F5 (nit) `delete_keys_bounded` spawns `min(concurrency, keys)` OS threads against the single current-thread runtime: 128 threads for paginate cleanup at K=128. Fine per W48 / test-only, but a modest clamp (e.g. 32) would be gentler on S3 and the runner | CONFIRMED (`tests/s3_integration.rs` `delete_keys_bounded`: `workers = (concurrency as usize).min(keys.len()).max(1)` with no ceiling). Optional | W164 (optional; pure-fn RED + clamp GREEN if taken) |

### Positives (no action; acknowledged in W165 reply)

- `object_keys_from_raw` pure helper + three offline pins.
- Cleanup no longer pays the second N-head tax; delete safety preserved for trailing-`/` / exact-prefix marker.
- Condvar rendezvous with hard deadlock-panic timeout is a real determinism upgrade over `yield_now`.
- Merging `integration` / `integration-nightly` restores the nightly full-suite signal as a PR gate.

---

## Scope decisions

| ID | Question | Decision |
| -- | -------- | -------- |
| R23-f1 | F1 disposition: route comparison legs through `store.inner` vs document-only ordering constraint | **Route through `store.inner` (reviewer's preferred small fix), and make the pin order-independent.** The comparisons are about result equality, not gauging, so the rendezvous never needs to see them - same rationale already written on `enrich_parallel_vanished_warning_order_stable`. Document-only is rejected: a silent ordering constraint will bite the next contributor who adds a sequential baseline first. Do **not** change `OverlapRendezvous` itself (latch-once is correct for a single gauge pass); the bug is in how the two tests reuse the gauged wrapper across legs. |
| R23-f1-tdd | How to RED F1 under strict TDD without a flaky 5s wall-clock wait as the only signal | **Reorder both gauge tests so the concurrency-1 comparison leg runs FIRST against the gauged store, and assert equality + that the subsequent concurrency-N gauge leg still proves overlap.** Today that deadlocks/panics in ~5s (reviewer-verified; re-confirm as the RED observation). After GREEN (`store.inner` for the comparison leg), the same order stays as a permanent pin: conc-1-first can no longer latch-poison the gauge, and a future regression that re-routes the comparison through the gauge fails loudly again. Also drop the 5s wall from the RED loop where possible by using a short-timeout local probe only if needed - preferred path is to edit the two existing tests in place (one RED observation each) rather than add a third gauge. |
| R23-f2 | F2 scope: which untracked plans to commit | **Commit the two plans the review names and the PR/roadmap already link**: `doc/plans/issue-17.md`, `doc/plans/push-pagination-test-below-30s-total.md`. Other untracked plans on disk (`issue-8.md`, `issue-20.md`, `pr-16-...`, `pr-21-...`, `pr-22-...`, and this plan itself once written) are out of this finding's scope - land this plan (`pr-23-fix-5462873894.md`) in the same hygiene commit so the fix plan is tracked with the fix; leave the rest for their own PRs/branches. Refresh the PR body to 8 commits / P30 knobs / ~18-24s live totals (cite the five measured totals from the P30 roadmap row: 22729 / 24012 / 17835 / 18372 / 18167 ms). |
| R23-f3 | F3 disposition | **No code change.** Required status checks are not enabled on `main`; record the API evidence in the reply. Optionally fix the stale CI comment that still says "conc 32" while the test uses K=128 (same W163 docs pass - tiny, related hygiene). |
| R23-f4 | F4 disposition | **Wording only.** Replace "Measures get_to overlap (bytes streaming)" with "proves pool fan-out" (or equivalent). No behavior change. |
| R23-f5 | F5 disposition: take the optional clamp or defer | **Take it (small, test-only, matches reviewer suggestion).** Clamp cleanup delete workers at 32 via a named constant. Extract the worker-count line into a pure `cleanup_delete_workers(concurrency, n_keys) -> usize` helper next to `delete_keys_bounded` so it can be unit-tested offline inside the integration test module without hitting S3. Negligible wall-clock cost at 1050 keys (Phase 0 row D deletes were ~11s at 32 workers already). |

---

## Work items

### W160 - F1 RED: order-independent gauge pins (comparison leg first)

Edit the two existing gauge tests so the concurrency-1 *comparison* leg runs **before** the concurrency-N gauge leg, still through the gauged store (the buggy shape). Confirm RED:

1. `src/store/mod.rs` `enrich_heads_bounded_parallel`:
   - Seed 32 objects on `store.inner` as today.
   - **First** call `enrich_with_head_mtimes(&store, listing.clone(), 1)` and keep the result.
   - **Then** call `enrich_with_head_mtimes(&store, listing, 4)`, assert `max_in_flight() > 1` and `<= 4`, assert listings equal.
   - Expected RED today: panic at `OverlapRendezvous` after ~5s on the concurrency-1 leg (`max_in_flight=1 entered=1 target=2 n_workers=4`).
2. `src/exec.rs` `exec_parallel_downloads_overlap`:
   - Same reorder: concurrency-1 `execute_plan(..., 1)` against the gauged store first, then concurrency-4 gauge + equality.
   - Expected RED: same deadlock panic shape on the first `get_to` enter.

RED observation protocol: run each test alone once

```
cargo test --offline --lib enrich_heads_bounded_parallel -- --exact --nocapture
cargo test --offline --lib exec_parallel_downloads_overlap -- --exact --nocapture
```

Record the panic message in the commit message. Do **not** push the RED state.

Do not add a third test or change `OverlapRendezvous` in this step - the two existing tests become the pins.

### W161 - F1 GREEN: comparison legs use `store.inner`; comments tell why

Minimum fix to flip W160 GREEN:

1. `enrich_heads_bounded_parallel`: keep the conc-1-first order from W160; route the comparison leg through `&store.inner` (bare `MemoryStore`). Gauge leg stays on `&store` at concurrency 4. Assert `enriched4 == enriched1` as today.
2. `exec_parallel_downloads_overlap`: same - `execute_plan(&local1, &store.inner, ..., 1)` for the comparison leg; gauge leg stays on `&store` at concurrency 4. Bytes-on-disk asserts stay on the concurrency-4 vault; report equality `rep4 == rep1` unchanged.
3. Comments (both tests + both gauged-store wrappers, brief):
   - State that the concurrency-1 leg is an equality pin, not a gauge, and **must** use `store.inner` - the Condvar rendezvous latches `released` for the life of the wrapper, so a conc-1 pass through the gauge deadlocks (target=2 never reached; `n_workers` was sized for the N leg). Cross-reference `enrich_parallel_vanished_warning_order_stable`, which already documents the same trap.
   - Optionally one line on `OverlapRendezvous` itself: "single gauge pass per instance; do not reuse across a sequential baseline leg."
4. F4 can land in the same commit if touching `GaugedGetStore` comments anyway (see W163) - preferred: keep F4 in W163 so each finding's commit stays single-purpose; the F1 comment rewrite should not claim "bytes streaming".

Gate: both gauge tests green alone and in the full lib suite; 5x repeat of each gauge test green (matches the reviewer's flake check bar). One commit: `test: [17] gauge comparison legs use store.inner (no OverlapRendezvous order trap)`.

### W162 - F2 hygiene: commit linked plans + refresh PR body

No behavior change; all-green gate only.

1. Refresh `doc/plans/issue-17.md` status line if it still says tip `d39729d` - note Track A+B + P30 landed, tip at implementation time (and that PR 23 r1 fixes are tracked in `pr-23-fix-5462873894.md`).
2. Commit, on this branch:
   - `doc/plans/issue-17.md`
   - `doc/plans/push-pagination-test-below-30s-total.md`
   - `doc/plans/pr-23-fix-5462873894.md` (this plan)
3. Update the PR body via `gh pr edit 23 --body-file <tmp>` (developer capacity: `GH_TOKEN=$GH_TOKEN_TLKAHN`) so it matches tip:
   - 8 commits (add the P30 line: `test: [p30] list_paginates seed=64 K=128 under 30s bar`).
   - `with_store_conc("paginate", 128, ...)` / `seed_workers = 64` / K = 128.
   - Live full-suite / paginate totals: cite the P30 five-run band (~18-24s), not the pre-P30 ~54s figure, while keeping the Phase 0 A-D table as historical evidence.
   - Links to `doc/plans/issue-17.md` and `doc/plans/push-pagination-test-below-30s-total.md` remain, now live once committed.
4. Out of scope (do not sneak in): other untracked plans (`issue-8`, `issue-20`, `pr-16-*`, `pr-21-*`, `pr-22-*`).

One commit: `docs: [17] commit issue-17 + P30 plans; track PR 23 r1 fix plan`. PR body edit is not a commit (metadata only) - do it in this step after the push, or in W165; either is fine as long as it lands before the reply.

### W163 - F4 nit (+ related stale CI comment): wording only

1. `src/exec.rs` `GaugedGetStore` doc comment: replace "Measures get_to overlap (bytes streaming), not tmp allocation - ..." with wording that matches reality, e.g. "Proves pool fan-out on `get_to` entry (Condvar rendezvous arrivals), not concurrent byte streaming and not tmp allocation - the F3 `dir_create_lock` serializes create-alloc, so the rendezvous belongs here around the inner store call."
2. While touching CI-adjacent docs comments (optional same commit): `.github/workflows/ci.yml` still says "conc 32" / `with_store_conc(32)` in the integration job comments - bump to K=128 / seed note so the job description matches P30. No YAML behavior change.

One commit: `docs: [17] gauge comment proves pool fan-out; CI comments match K=128`.

### W164 - F5 optional: clamp cleanup delete fan-out at 32 (TDD)

Take per R23-f5. Strict TDD on a pure helper (no S3):

1. **RED** in `tests/s3_integration.rs` (module-level unit tests next to the harness helpers - they compile with the integration binary and run even when the bucket env is unset, same pattern as any pure helper tests already in that file; if none exist yet, add a small `#[cfg(test)]` / plain `#[test]` block at the bottom of the helpers section):
   - `cleanup_delete_workers_clamps_to_max`: `cleanup_delete_workers(128, 1050) == 32` (RED today if the helper is introduced as a passthrough of `min(concurrency, n_keys)`).
   - `cleanup_delete_workers_respects_key_count`: `cleanup_delete_workers(128, 4) == 4`.
   - `cleanup_delete_workers_at_least_one`: `cleanup_delete_workers(0, 10) == 1` or whatever the current `.max(1)` contract is - keep behavior identical except for the new ceiling.
2. **GREEN**:
   ```rust
   const MAX_CLEANUP_DELETE_WORKERS: usize = 32; // I17-r1/F5: gentle on S3 + runner
   fn cleanup_delete_workers(concurrency: u32, n_keys: usize) -> usize {
       (concurrency as usize)
           .min(n_keys)
           .min(MAX_CLEANUP_DELETE_WORKERS)
           .max(1)
   }
   ```
   `delete_keys_bounded` uses `cleanup_delete_workers(concurrency, keys.len())` for the worker count. Comment cites F5 / W164 and that product `run_bounded` is unchanged.
3. Gate: new unit tests green via `cargo test --locked --test s3_integration cleanup_delete_workers -- --nocapture` (bucket-unset path must still run these pure tests - if the file's current structure skips the whole binary without a bucket, place the pure tests so they are not behind the skip; they are plain `#[test]` fns and always register). Full lib suite unaffected.

One commit: `test: [17] clamp cleanup delete fan-out at 32 (F5)`.

If during implementation the pure tests cannot run without the bucket env due to some file-level harness quirk, fall back to putting `cleanup_delete_workers` in a tiny `#[cfg(test)]` module under `src/` only if necessary - prefer keeping it test-local. **Do not** change production `pool::run_bounded`.

### W165 - decision log, full gate, push, reply

1. `doc/roadmap.md` decision log: one dated row `I17-r1` - PR 23 review round 1 (issuecomment-5462873894): F1 gauge comparison legs routed through `store.inner` so `OverlapRendezvous` latch-once cannot order-trap a sequential baseline (W160/W161); F2 linked plans committed + PR body refreshed to P30 knobs (W162); F3 required-status-checks verified absent on `main` (no CI rename fallout); F4 gauge comment restated as pool fan-out (W163); F5 cleanup delete workers clamped at 32 (W164).
2. Full gate green: `cargo fmt --check` + `cargo clippy --all-targets --locked -- -D warnings` + `cargo test --locked --lib --bins` + the new `cleanup_delete_workers_*` tests. Push to the PR branch; confirm `gh pr checks 23` green (including `integration (S3)` under the post-rename name).
3. If not done in W162: `gh pr edit 23 --body-file <tmp>` with the refreshed body.
4. Reply to issuecomment-5462873894 mapping findings to dispositions. Write the body to a temp file first and post via `--body-file` (never heredoc/inline), in developer capacity:

   ```
   GH_TOKEN=$GH_TOKEN_TLKAHN gh pr comment 23 --body-file <tmp>
   ```

   Reply content (outline):
   - F1 -> fixed (W160/W161): comparison legs use `store.inner`; conc-1-first pin kept so the trap cannot return silently.
   - F2 -> fixed (W162): plans committed; PR body matches tip (8 commits, seed=64, K=128, ~18-24s).
   - F3 -> verified no action: required status checks not enabled on `main` (API 404); current checks already report `integration (S3)`.
   - F4 -> fixed (W163): comment now says pool fan-out.
   - F5 -> taken (W164): `MAX_CLEANUP_DELETE_WORKERS = 32` with pure offline pins.
   - Positives acknowledged.

---

## Reviewer findings -> this plan

- F1 -> W160 (RED reorder pin) + W161 (GREEN `store.inner`) - fix, per R23-f1.
- F2 -> W162 (commit plans + PR body) - hygiene.
- F3 -> W165 reply only - verified safe, per R23-f3.
- F4 -> W163 (comment) - nit.
- F5 -> W164 (optional clamp, taken) - pure-fn TDD + harness clamp, per R23-f5.
- Positives -> acknowledged in W165's reply.

## Commit order

W160+W161 (one commit, F1) -> W164 (one commit, F5, if taken before docs so behavior commits lead) -> W163 (one commit, F4 wording) -> W162 (one commit, plans + this plan file) -> W165 (decision-log commit if not folded into W162, then gate / push / PR body / reply).

Suggested practical sequencing:

1. **F1 commit** (W160 RED locally, then W161 GREEN) - the only merge-blocking finding.
2. **F5 commit** (W164 RED helper tests, then GREEN clamp) - small behavior/test-harness change.
3. **F4 commit** (W163 wording + CI comment freshness).
4. **Hygiene commit** (W162 plans + W165 decision-log row together is acceptable: one docs commit).
5. **Close-out** (gate, push, PR body edit, reply).

Rationale: F1 first by severity (reviewer: "approve once F1 is addressed"); F5 next while still in test code; wording and plan-doc commits are zero-risk close-out and must not bury a behavior fix in a docs-only hash.

## Explicit non-goals

- No change to `OverlapRendezvous` release / latch semantics (correct for a single gauge pass).
- No re-`#[ignore]` of the gauge tests.
- No product-path changes to `enrich_with_head_mtimes`, `execute_plan`, `run_bounded`, or `S3Store`.
- No `DeleteObjects` bulk API (still out of scope per issue-17).
- No committing unrelated untracked plans.
- No branch-protection API mutations (F3 is verify-only).

## Done when

- Both gauge tests pass with concurrency-1 comparison leg first, via `store.inner`.
- `doc/plans/issue-17.md`, `doc/plans/push-pagination-test-below-30s-total.md`, and this plan are tracked on the branch.
- PR body matches tip (P30 knobs, 8 commits, live timing band).
- F3 verification recorded in the reply.
- F4 comment corrected; F5 clamp at 32 with pure offline pins.
- Full gate green; reply posted mapping F1-F5 to dispositions.
